import { styled } from '@styled-system/jsx'

export const Board = styled('div', {
  base: {
    display: 'flex',
    padding: '0 30px',
    marginTop: '8',
    height: 'calc(100% - 50px)',
    gap: '2',
  },
})
